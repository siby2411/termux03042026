<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OMEGA CONSULTING - SYSCOHADA 2026</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root { --omega-dark: #00264d; --omega-gold: #c5a059; --omega-gradient: linear-gradient(135deg, #00264d 0%, #00509d 100%); }
        body { background-color: #f0f2f5; font-family: 'Inter', sans-serif; color: #333; }
        .omega-banner { 
            background: var(--omega-gradient); 
            color: white; 
            padding: 40px 20px; 
            border-bottom: 6px solid var(--omega-gold);
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 40px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
        .omega-card { 
            border: none; 
            border-radius: 20px; 
            box-shadow: 0 15px 35px rgba(0,0,0,0.1); 
            background: white;
            transition: transform 0.3s ease;
        }
        .form-centered { max-width: 950px; margin: 0 auto; }
        .btn-omega { 
            background: var(--omega-gradient); 
            border: none; 
            color: white; 
            padding: 12px 30px; 
            border-radius: 50px;
            font-weight: 600;
            letter-spacing: 1px;
        }
        .btn-omega:hover { transform: scale(1.05); color: var(--omega-gold); }
        .form-label { font-weight: 600; color: var(--omega-dark); margin-bottom: 8px; }
        .form-control { border-radius: 10px; border: 1px solid #ddd; padding: 12px; }
        .form-control:focus { border-color: var(--omega-gold); box-shadow: 0 0 0 0.25 margin:rgba(197, 160, 89, 0.25); }
    </style>
</head>
<body>

<div class="omega-banner text-center">
    <div class="container">
        <h1 class="display-5 fw-bold"><i class="bi bi-bank2"></i> OMEGA INFORMATIQUE CONSULTING</h1>
        <p class="lead mb-0">Solution Intégrée de Gestion Comptable & Financière - UEMOA 2026</p>
        <hr class="w-25 mx-auto bg-light">
    </div>
</div>

<div class="container">
